spring:
  datasource:
    url: jdbc:h2:mem:academia_dev
    driverClassName: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never

jwt:
  secret: dev-secret-de-32-caracteres-minimo
  expiration: 86400000
  refresh-expiration: 604800000

logging:
  level:
    co.udistrital.academia: DEBUG
    org.hibernate.SQL: DEBUG
